# Kuang Ting (Tim) Tu - GitHub Pages Website

Static personal portfolio site for GitHub Pages.

## Local Preview

Open `index.html` directly in a browser, or serve the directory with any static file server.

## GitHub Pages Deployment

Use a repository named `<github-username>.github.io`, push this source to the `main` branch, then enable Pages from the repository settings if it is not enabled automatically.
